import * as React from 'react';
import {Appbar, BottomNavigationBar} from '../../components';
import {StyleSheet, Text} from 'react-native';

const MainScreen = () => {
  return <Text> ANA EKRAN </Text>;
};

export default MainScreen;
